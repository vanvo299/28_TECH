/*
- Phương pháp quay lui
*/

#include <bits/stdc++.h>
using namespace std;

int x[100], n;

void Try(int i) 
{
    // Thử gán các phần tử có thể gán cho x[i]
    /*
    for (j = <Giá trị 1> .... <Giá trị m) {

        <Kiểm tra xem có gán được j cho x[i] hay không ?>
        x[i] = j;
        <Kiểm tra xem i có phải là phần tử cuối cùng của cấu hình hay không ?>
    
    if (i == n) {
        <Tìm được một cấu hình>
    }
    else {
        <Tiếp tục xây dựng phần tử thứu i + 1>
        Try(i + 1);
    }
    <Back Track>
    <Bỏ ghi nhận X[i] = j>
}

    */
}

int main()
{

    return 0;
}